// 'use strict';

// const { Sequelize, DataTypes } = require('sequelize');
// const peopleSchema = require('./people.schema');

// const DATABASE_URL = process.env.NODE_ENV === 'test'
//     ? 'sqlite::memory'
//     : process.env.DATABASE_URL || 'postgres://localhost:5432/401-api-app';

// const DATABASE_URL = process.env.DATABASE_URL || 'postgres://localhost:5432/401-api-app';

// const sequelize = new Sequelize(DATABASE_URL);

// const PeopleModel = peopleSchema(sequelize, DataTypes);

// module.exports = {
//     sequelize,
//     PeopleModel,
// };
